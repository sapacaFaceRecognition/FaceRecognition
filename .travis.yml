language: java

jdk:
- oraclejdk8

# only build these branches (RegEx is also valid, eg. /^deploy.*$/):
branches:
  only:
    - master

# container-based build:
sudo: false

before_script: cd complete

script: ./gradlew check